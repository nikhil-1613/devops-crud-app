apiVersion: v2
name: devops-crud-app
description: >
  A full-stack task management application with a React frontend,
  Node.js/Express backend, and PostgreSQL database. Includes HPA,
  Ingress, Secrets, and ConfigMaps for production-ready Kubernetes deployment.

type: application

# Chart version — increment this every time you change the chart templates or values.
version: 0.1.0

# Application version — matches the Docker image tag being deployed.
appVersion: "1.0.0"
